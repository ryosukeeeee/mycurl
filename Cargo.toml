[package]
name = "mycurl"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4.7", features = ["derive"] }
# 非同期処理の解説をしてないので、今回はブロッキングクライアントを使う 
reqwest = { version = "0.11.22", features = ["blocking"] }
